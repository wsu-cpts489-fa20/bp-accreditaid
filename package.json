{
  "name": "code-ch18-eb",
  "version": "1.0.0",
  "description": "Testing instructions for deployment to EB",
  "main": "server.compiled.js",
  "scripts": {
    "start": "(cd client && npm install && npm run build && cd .. && node server.js);",
    "build": "npm run build-server && npm run build-client",
    "build-server": "babel server.js --out-file server.compiled.js",
    "build-client": "cd client/ && npm run build",
    "start:dev": "npm run build && nodemon --config nodemon.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "C Hundhausen",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-session": "^1.17.1",
    "mongoose": "^5.10.9",
    "passport": "^0.4.1",
    "passport-github": "^1.1.0",
    "passport-local": "^1.0.0",
    "path": "^0.12.7",
    "regenerator-runtime": "^0.13.7"
  },
  "devDependencies": {
    "@babel/cli": "^7.12.1",
    "@babel/core": "^7.12.1",
    "@babel/node": "^7.12.1",
    "@babel/preset-env": "^7.12.1",
    "nodemon": "^2.0.6"
  }
}
